# Copie este arquivo para `.env.local` para desenvolvimento e ajuste os valores conforme necessário.
# Em produção, defina as variáveis diretamente na plataforma (DigitalOcean App Platform ou Droplet).

# Banco de dados
# Para desenvolvimento local (SQLite):
DATABASE_URL="file:./prisma/dev.db"
# Para produção (PostgreSQL - descomentar e ajustar se necessário):
# DATABASE_URL="postgresql://user:password@host:5432/airx_prod"

# Ajuste para "false" em produção para bloquear auto-registro de contas.
ALLOW_SELF_SIGNUP="true"

# Autenticação (gerar com: openssl rand -base64 32)
NEXTAUTH_SECRET="seu-secret-aleatorio-aqui"
NEXTAUTH_URL="http://localhost:3000"

# Variáveis opcionais para o script de seed de usuários de teste
# AIRX_TEST_EMAIL="editor@airx.dev"
# AIRX_TEST_NAME="Editor Piloto"
# AIRX_TEST_ROLE="CONTROLLER"
# AIRX_TEST_PASSWORD="mude-esta-senha"

# ====================
# Sistema de Emails (Resend)
# ====================
# Obter API Key em: https://resend.com
RESEND_API_KEY="re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

# Email de origem (precisa configurar domínio no Resend)
EMAIL_FROM="Air X Control <noreply@seudominio.com>"

# URL pública do app (para links nos emails)
NEXT_PUBLIC_APP_URL="https://air-x-control-9tnmi.ondigitalocean.app"
